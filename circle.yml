machine:
  node:
    version: 7.0.0

test:
  override:
    - mocha test --reporter mocha-junit-reporter --no-timeouts:
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml
